# Image_Processing_Tasks
The code processes an image by converting it to grayscale, rotating it by 45 degrees, creating a 5-level image pyramid, magnifying the 3rd pyramid level to its original size using bilinear interpolation, and computing the difference between the original and magnified images, saving all results.
